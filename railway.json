{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ReferralLinkPlatform/backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd ReferralLinkPlatform/backend && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}